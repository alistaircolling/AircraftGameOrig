<?xml version="1.0" encoding="utf-8"?>
<s:WindowedApplication xmlns:fx="http://ns.adobe.com/mxml/2009" 
					   xmlns:s="library://ns.adobe.com/flex/spark" 
					   xmlns:mx="library://ns.adobe.com/flex/mx" xmlns:components="view.components.*" xmlns:local="*"
					   creationComplete="windowedapplication1_creationCompleteHandler(event)"
					   width="1024" height="768"
					   >
	<fx:Style source="AircraftGame.css"/>
	<fx:Script>
		<![CDATA[
			import flash.debugger.enterDebugger;
			
			import flashx.textLayout.events.StatusChangeEvent;
			
			import mx.events.FlexEvent;
			
			import signals.ChangeState;

			private var context:ProjectContext;
			
			protected function windowedapplication1_creationCompleteHandler(event:FlexEvent):void
			{
				context = new ProjectContext(this, true);
				updateState(ChangeState.INTRO_SCREEN);
			}
			
			
			public function updateState( s:String ):void{
				
				currentState = s;
				trace("changing state to:"+s);
				//add any additional logic here
				switch(currentState){
					case ChangeState.INTRO_SCREEN:
						introView.visible = true;
						inputView.visible = false;
						break
					
					case ChangeState.ENTER_SCREEN:
						inputView.visible = true;
						resultsView.visible = false;
						
						break
					case ChangeState.RESULTS_SCREEN:
						resultsView.visible = true;
						inputView.visible = false;
						finalView.visible = false;
						break
					case ChangeState.FINAL_SCREEN:
						resultsView.visible = false;
						inputView.visible = false;
						finalView.visible = true;
						exitView.visible = false;
						break
					case ChangeState.EXIT_SCREEN:
						exitView.visible = true;
						finalView.visible = false;
						introView.visible = false;
						break
					default:
						
						break
				}
				
				
			}

		]]>
	</fx:Script>
	
	<fx:Declarations>
		<!-- <local:ProjectContext contextView="{this}"/> -->
	</fx:Declarations>
	<s:states>
		<s:State name="intro" />
		<s:State name="enter" />
		<s:State name="resultsScreen" />
		<s:State name="finalScreen" />
		<s:State name="exitScreen" />
	</s:states>
	<components:InputView id="inputView" includeIn="enter, intro, resultsScreen" visible="false"/> <!-- input wont show correctly as it isnt visible when the data is first recived need to fix TODO -->
	<components:IntroView id="introView" includeIn="intro, exitScreen"/>
	<components:ResultsView id="resultsView" includeIn="resultsScreen, enter, finalScreen" visible="false" horizontalCenter="0"/>
	<components:FinalView id="finalView" includeIn="resultsScreen, finalScreen, enter" visible="false" horizontalCenter="0" verticalCenter="0"/>
	<components:ExitView id="exitView" includeIn="finalScreen, exitScreen" visible="false" horizontalCenter="0" verticalCenter="0"/>
	<s:TextArea id="statusLabel" fontSize="12" bottom="5" left="5"  height="100" width="400"/>
	<s:Label id="gameID" text="GameID: --- " right="10" top="10" fontSize="20" fontWeight="bold" color="0xff0000"/>
</s:WindowedApplication>
